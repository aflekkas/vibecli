---
description: Survey peer Ink + AI-SDK CLI tools and report parity gaps and steal-worthy patterns for vibecli.
argument-hint: [feature-area]
---

Delegate to the `feature-parity` agent.

Focus area: **$ARGUMENTS** (or "overall surface" if empty).

Compare against the agent's default peer set (opencode, claude code, codex, gemini-cli, aider) plus any tool the user names. Return: gaps (capabilities multiple peers ship that vibecli does not), steal-worthy patterns (better APIs/defaults), out-of-scope items (peer features that violate the boundary), and 1-3 ranked next-move recommendations.

Read-only. Cite primary sources for every claim.
